// Generated by tkgen - Version Mar  4 2013
// tkgen was developed by Thiago Adams
// www.thradams.com
//
// Copyright (C) 2009, Thiago Adams (thiago.adams@gmail.com)
// http://www.thradams.com/
// Permission to copy, use, modify, sell and distribute this software
// is granted provided this copyright notice appears in all copies.
// This software is provided "as is" without express or implied
// warranty, and with no claim as to its suitability for any purpose.
//

#ifndef __SAMPLE1DFA__
#define __SAMPLE1DFA__

namespace Sample1
{

enum Tokens
{
    TKKeywordEmpty,
    TKKeywordAny,
    TKKeywordModule,
    TKKeywordLanguage,
    TKKeywordInterleave,
    TKKeywordSyntax,
    TKKeywordToken,
    TKPlusCharacter,
    TKLiteralChar,
    TKRangeSeparator,
    TKEqualsCharacter,
    TKSemicolonCharacter,
    TKCommaCharacter,
    TKIdentifier,
    TKQuestionMarkCharacter,
    TKAsteriskCharacter,
    TKLeftCurlyBracketCharacter,
    TKRightCurlyBracketCharacter,
    TKVerticalLineCharacter,
    TKAmpersandCharacter,
    TKLeftParenthesisCharacter,
    TKRightParenthesisCharacter,
    TKStringLiteral,
};

inline const wchar_t* TokensToString(Tokens e)
{
    switch(e)
    {
    case TKKeywordEmpty: return L"KeywordEmpty";
    case TKKeywordAny: return L"KeywordAny";
    case TKKeywordModule: return L"KeywordModule";
    case TKKeywordLanguage: return L"KeywordLanguage";
    case TKKeywordInterleave: return L"KeywordInterleave";
    case TKKeywordSyntax: return L"KeywordSyntax";
    case TKKeywordToken: return L"KeywordToken";
    case TKPlusCharacter: return L"PlusCharacter";
    case TKLiteralChar: return L"LiteralChar";
    case TKRangeSeparator: return L"RangeSeparator";
    case TKEqualsCharacter: return L"EqualsCharacter";
    case TKSemicolonCharacter: return L"SemicolonCharacter";
    case TKCommaCharacter: return L"CommaCharacter";
    case TKIdentifier: return L"Identifier";
    case TKQuestionMarkCharacter: return L"QuestionMarkCharacter";
    case TKAsteriskCharacter: return L"AsteriskCharacter";
    case TKLeftCurlyBracketCharacter: return L"LeftCurlyBracketCharacter";
    case TKRightCurlyBracketCharacter: return L"RightCurlyBracketCharacter";
    case TKVerticalLineCharacter: return L"VerticalLineCharacter";
    case TKAmpersandCharacter: return L"AmpersandCharacter";
    case TKLeftParenthesisCharacter: return L"LeftParenthesisCharacter";
    case TKRightParenthesisCharacter: return L"RightParenthesisCharacter";
    case TKStringLiteral: return L"StringLiteral";
    default:break;
    }
    return L"";
}

struct DFA
{
    typedef Tokens TokenType;

    static int GetNext(int state, wchar_t ch)
    {
        switch (state)
        {
            case 0:
            if (ch == L'"')
                return 1;
            else if (ch == L'$')
                return 2;
            else if (ch == L'&')
                return 3;
            else if (ch == L'\'')
                return 4;
            else if (ch == L'(')
                return 5;
            else if (ch == L')')
                return 6;
            else if (ch == L'*')
                return 7;
            else if (ch == L'+')
                return 8;
            else if (ch == L',')
                return 9;
            else if (ch == L'.')
                return 10;
            else if (ch == L';')
                return 11;
            else if (ch == L'=')
                return 12;
            else if (ch == L'?')
                return 13;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
            else if (ch == L'a')
                return 14;
        else if (ch >= L'b' && ch <= L'd')
          return 2;
            else if (ch == L'e')
                return 15;
        else if (ch >= L'f' && ch <= L'h')
          return 2;
            else if (ch == L'i')
                return 16;
        else if (ch >= L'j' && ch <= L'k')
          return 2;
            else if (ch == L'l')
                return 17;
            else if (ch == L'm')
                return 18;
        else if (ch >= L'n' && ch <= L'r')
          return 2;
            else if (ch == L's')
                return 19;
            else if (ch == L't')
                return 20;
        else if (ch >= L'u' && ch <= L'z')
          return 2;
            else if (ch == L'{')
                return 21;
            else if (ch == L'|')
                return 22;
        else if (ch == L'}')
          return 23;
    break; // 
            case 1:
        if (ch >= L' ' && ch <= L'!')
          return 24;
            else if (ch == L'"')
                return 25;
        else if (ch >= L'#' && ch <= L'~')
           return 24;
    break; // 
            case 2:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 3:
        //TKAmpersandCharacter
    break; // 
            case 4:
        if (ch >= L' ' && ch <= L'&')
          return 28;
            else if (ch == L'\'')
                return 29;
        else if (ch >= L'(' && ch <= L'~')
           return 28;
    break; // 
            case 5:
        //TKLeftParenthesisCharacter
    break; // 
            case 6:
        //TKRightParenthesisCharacter
    break; // 
            case 7:
        //TKAsteriskCharacter
    break; // 
            case 8:
        //TKPlusCharacter
    break; // 
            case 9:
        //TKCommaCharacter
    break; // 
            case 10:
        if (ch == L'.')
          return 30;
    break; // 
            case 11:
        //TKSemicolonCharacter
    break; // 
            case 12:
        //TKEqualsCharacter
    break; // 
            case 13:
        //TKQuestionMarkCharacter
    break; // 
            case 14:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'm')
          return 2;
            else if (ch == L'n')
                return 31;
        else if (ch >= L'o' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 15:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'l')
          return 2;
            else if (ch == L'm')
                return 32;
        else if (ch >= L'n' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 16:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'm')
          return 2;
            else if (ch == L'n')
                return 33;
        else if (ch >= L'o' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 17:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
            else if (ch == L'a')
                return 34;
        else if (ch >= L'b' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 18:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'n')
          return 2;
            else if (ch == L'o')
                return 35;
        else if (ch >= L'p' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 19:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'x')
          return 2;
            else if (ch == L'y')
                return 36;
            else if (ch == L'z')
                return 2;
        //TKIdentifier
    break; // 
            case 20:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'n')
          return 2;
            else if (ch == L'o')
                return 37;
        else if (ch >= L'p' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 21:
        //TKLeftCurlyBracketCharacter
    break; // 
            case 22:
        //TKVerticalLineCharacter
    break; // 
            case 23:
        //TKRightCurlyBracketCharacter
    break; // 
            case 24:
  return 1; //any
    break; // 
            case 25:
        //TKStringLiteral
    break; // 
            case 26:
            if (ch == L'$')
                return 38;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 38;
            else if (ch == L'_')
                return 38;
        else if (ch >= L'a' && ch <= L'z')
           return 38;
    break; // 
            case 27:
  return -1;
    break; // 
            case 28:
  return 4; //any
    break; // 
            case 29:
        //TKLiteralChar
    break; // 
            case 30:
        //TKRangeSeparator
    break; // 
            case 31:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'x')
          return 2;
            else if (ch == L'y')
                return 39;
            else if (ch == L'z')
                return 2;
        //TKIdentifier
    break; // 
            case 32:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'o')
          return 2;
            else if (ch == L'p')
                return 40;
        else if (ch >= L'q' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 33:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L's')
          return 2;
            else if (ch == L't')
                return 41;
        else if (ch >= L'u' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 34:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'm')
          return 2;
            else if (ch == L'n')
                return 42;
        else if (ch >= L'o' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 35:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'c')
          return 2;
            else if (ch == L'd')
                return 43;
        else if (ch >= L'e' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 36:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'm')
          return 2;
            else if (ch == L'n')
                return 44;
        else if (ch >= L'o' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 37:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'j')
          return 2;
            else if (ch == L'k')
                return 45;
        else if (ch >= L'l' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 38:
            if (ch == L'$')
                return 38;
        else if (ch >= L'A' && ch <= L'Z')
          return 38;
            else if (ch == L'_')
                return 38;
        else if (ch >= L'a' && ch <= L'z')
          return 38;
        //TKIdentifier
    break; // 
            case 39:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'z')
          return 2;
        //TKKeywordAny
        //TKKeywordAny
    break; // 
            case 40:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L's')
          return 2;
            else if (ch == L't')
                return 46;
        else if (ch >= L'u' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 41:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'd')
          return 2;
            else if (ch == L'e')
                return 47;
        else if (ch >= L'f' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 42:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'f')
          return 2;
            else if (ch == L'g')
                return 48;
        else if (ch >= L'h' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 43:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L't')
          return 2;
            else if (ch == L'u')
                return 49;
        else if (ch >= L'v' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 44:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L's')
          return 2;
            else if (ch == L't')
                return 50;
        else if (ch >= L'u' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 45:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'd')
          return 2;
            else if (ch == L'e')
                return 51;
        else if (ch >= L'f' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 46:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'x')
          return 2;
            else if (ch == L'y')
                return 52;
            else if (ch == L'z')
                return 2;
        //TKIdentifier
    break; // 
            case 47:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'q')
          return 2;
            else if (ch == L'r')
                return 53;
        else if (ch >= L's' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 48:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L't')
          return 2;
            else if (ch == L'u')
                return 54;
        else if (ch >= L'v' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 49:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'k')
          return 2;
            else if (ch == L'l')
                return 55;
        else if (ch >= L'm' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 50:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
            else if (ch == L'a')
                return 56;
        else if (ch >= L'b' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 51:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'm')
          return 2;
            else if (ch == L'n')
                return 57;
        else if (ch >= L'o' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 52:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'z')
          return 2;
        //TKKeywordEmpty
        //TKKeywordEmpty
    break; // 
            case 53:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'k')
          return 2;
            else if (ch == L'l')
                return 58;
        else if (ch >= L'm' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 54:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
            else if (ch == L'a')
                return 59;
        else if (ch >= L'b' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 55:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'd')
          return 2;
            else if (ch == L'e')
                return 60;
        else if (ch >= L'f' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 56:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'w')
          return 2;
            else if (ch == L'x')
                return 61;
        else if (ch >= L'y' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 57:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'z')
          return 2;
        //TKKeywordToken
        //TKKeywordToken
    break; // 
            case 58:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'd')
          return 2;
            else if (ch == L'e')
                return 62;
        else if (ch >= L'f' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 59:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'f')
          return 2;
            else if (ch == L'g')
                return 63;
        else if (ch >= L'h' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 60:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'z')
          return 2;
        //TKKeywordModule
        //TKKeywordModule
    break; // 
            case 61:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'z')
          return 2;
        //TKKeywordSyntax
        //TKKeywordSyntax
    break; // 
            case 62:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
            else if (ch == L'a')
                return 64;
        else if (ch >= L'b' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 63:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'd')
          return 2;
            else if (ch == L'e')
                return 65;
        else if (ch >= L'f' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 64:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'u')
          return 2;
            else if (ch == L'v')
                return 66;
        else if (ch >= L'w' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 65:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'z')
          return 2;
        //TKKeywordLanguage
        //TKKeywordLanguage
    break; // 
            case 66:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'd')
          return 2;
            else if (ch == L'e')
                return 67;
        else if (ch >= L'f' && ch <= L'z')
          return 2;
        //TKIdentifier
    break; // 
            case 67:
            if (ch == L'$')
                return 2;
        else if (ch >= L'0' && ch <= L'9')
          return 26;
        else if (ch >= L'A' && ch <= L'Z')
          return 2;
            else if (ch == L'_')
                return 2;
        else if (ch >= L'a' && ch <= L'z')
          return 2;
        //TKKeywordInterleave
        //TKKeywordInterleave
    break; // 
        } //switch
        return -1;
    }

    static bool IsInterleave(Tokens tk)
    {
        return false; //todo add you blanks token here
    }

    static bool GetTokenFromState(int state, Tokens& tk)
    {
        switch(state)
        {
            case 2: tk = TKIdentifier; break;
            case 3: tk = TKAmpersandCharacter; break;
            case 5: tk = TKLeftParenthesisCharacter; break;
            case 6: tk = TKRightParenthesisCharacter; break;
            case 7: tk = TKAsteriskCharacter; break;
            case 8: tk = TKPlusCharacter; break;
            case 9: tk = TKCommaCharacter; break;
            case 11: tk = TKSemicolonCharacter; break;
            case 12: tk = TKEqualsCharacter; break;
            case 13: tk = TKQuestionMarkCharacter; break;
            case 14: tk = TKIdentifier; break;
            case 15: tk = TKIdentifier; break;
            case 16: tk = TKIdentifier; break;
            case 17: tk = TKIdentifier; break;
            case 18: tk = TKIdentifier; break;
            case 19: tk = TKIdentifier; break;
            case 20: tk = TKIdentifier; break;
            case 21: tk = TKLeftCurlyBracketCharacter; break;
            case 22: tk = TKVerticalLineCharacter; break;
            case 23: tk = TKRightCurlyBracketCharacter; break;
            case 25: tk = TKStringLiteral; break;
            case 29: tk = TKLiteralChar; break;
            case 30: tk = TKRangeSeparator; break;
            case 31: tk = TKIdentifier; break;
            case 32: tk = TKIdentifier; break;
            case 33: tk = TKIdentifier; break;
            case 34: tk = TKIdentifier; break;
            case 35: tk = TKIdentifier; break;
            case 36: tk = TKIdentifier; break;
            case 37: tk = TKIdentifier; break;
            case 38: tk = TKIdentifier; break;
            case 39: tk = TKKeywordAny; break;
            case 40: tk = TKIdentifier; break;
            case 41: tk = TKIdentifier; break;
            case 42: tk = TKIdentifier; break;
            case 43: tk = TKIdentifier; break;
            case 44: tk = TKIdentifier; break;
            case 45: tk = TKIdentifier; break;
            case 46: tk = TKIdentifier; break;
            case 47: tk = TKIdentifier; break;
            case 48: tk = TKIdentifier; break;
            case 49: tk = TKIdentifier; break;
            case 50: tk = TKIdentifier; break;
            case 51: tk = TKIdentifier; break;
            case 52: tk = TKKeywordEmpty; break;
            case 53: tk = TKIdentifier; break;
            case 54: tk = TKIdentifier; break;
            case 55: tk = TKIdentifier; break;
            case 56: tk = TKIdentifier; break;
            case 57: tk = TKKeywordToken; break;
            case 58: tk = TKIdentifier; break;
            case 59: tk = TKIdentifier; break;
            case 60: tk = TKKeywordModule; break;
            case 61: tk = TKKeywordSyntax; break;
            case 62: tk = TKIdentifier; break;
            case 63: tk = TKIdentifier; break;
            case 64: tk = TKIdentifier; break;
            case 65: tk = TKKeywordLanguage; break;
            case 66: tk = TKIdentifier; break;
            case 67: tk = TKKeywordInterleave; break;
            default:
                return false;
        }
        return true;
    }
};
} //namespace
#endif //header
